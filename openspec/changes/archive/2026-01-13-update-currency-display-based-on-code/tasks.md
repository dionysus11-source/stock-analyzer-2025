## 1. 데이터 분석기 수정 (`data_analyzer.py`)
- [x] 1.1 `_find_columns` 메서드를 수정하여 '코드' 열을 찾도록 합니다.
- [x] 1.2 `_calculate_results` 메서드를 수정하여 그룹화된 결과에 '코드' 값을 유지하거나, 코드 유형(예: 'USD', 'KRW')을 결정하여 포함시킵니다.
- [x] 1.3 `analyze_csv`의 반환 값에 통화 정보(종목별)를 추가합니다. 예: `(종목명, 평가손익, 매도 횟수, 통화)`
- [x] 1.4 `get_statistics`를 수정하여 혼합된 통화의 총계를 처리할 수 있도록 합니다 (예: 통화별로 총계를 계산).

## 2. UI 수정 (`ui_components.py`)
- [x] 2.1 `update_results` 메서드를 수정하여 `analyze_csv`로부터 받은 통화 정보에 따라 평가손익을 포맷팅합니다 (달러 또는 원).
- [x] 2.2 `update_results`에서 통계 정보를 표시하는 `stats_label`을 수정하여 혼합 통화 총계를 올바르게 표시합니다.

## 3. 사양 (Spec) 작성
- [x] 3.1 `data-analyzer`에 대한 신규 사양을 작성합니다. (`ADDED Requirements`)
- [x] 3.2 `ui-components`에 대한 수정 사양을 작성합니다. (`MODIFIED Requirements`)

## 4. 테스트
- [x] 4.1 한국 주식만 있는 CSV 파일로 테스트하여 '원'으로 표시되는지 확인합니다.
- [x] 4.2 미국 주식만 있는 CSV 파일로 테스트하여 '$'로 표시되는지 확인합니다.
- [x] 4.3 두 종류의 주식이 혼합된 CSV 파일로 테스트하여 각 행 및 총계가 올바르게 표시되는지 확인합니다.
