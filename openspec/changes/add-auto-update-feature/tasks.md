## 1. 버전 관리 설정
- [x] `_version.py` 파일에 `__version__` 변수를 정의하고 관리합니다.

## 2. 업데이트 로직 구현 (`auto_updater.py`)
- [x] 현재 애플리케이션 버전을 `_version.py`에서 가져오는 함수를 구현합니다.
- [x] Github API를 호출하여 최신 릴리즈의 버전 태그와 다운로드 URL을 가져오는 함수를 구현합니다.
- [x] 두 버전을 비교하여 업데이트가 필요한지 판단하는 함수를 구현합니다.
- [x] 릴리즈 자산(압축 파일)을 임시 폴더에 다운로드하고 압축을 해제하는 함수를 구현합니다.
- [x] 파일 복사, 프로세스 재시작 등의 작업을 수행할 `update.bat` 배치 파일을 생성하는 함수를 구현합니다.
- [x] 생성된 `update.bat`을 실행하는 함수를 구현합니다.

## 3. 메인 애플리케이션에 통합
- [x] `main.py`의 시작 부분에서 `auto_updater`의 업데이트 확인 함수를 호출하도록 통합합니다.
- [x] 사용자에게 업데이트 여부를 묻는 UI(예: 메시지 박스)를 추가합니다.

## 4. 빌드 및 테스트
- [x] `make_exe_file.bat` 스크립트가 `_version.py`를 올바르게 포함하여 빌드하는지 확인합니다.
- [x] 테스트용 Github 저장소 또는 로컬 환경에서 전체 업데이트 프로세스가 정상적으로 작동하는지 테스트합니다.
